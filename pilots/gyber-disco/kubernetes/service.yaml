apiVersion: v1
kind: Service
metadata:
  name: llama-auth-proxy
  namespace: workers
spec:
  selector:
    app.kubernetes.io/name: llama-auth-proxy
  ports:
    - name: http
      port: 8080
      targetPort: http
